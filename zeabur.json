{
  "build": {
    "command": "npm install && npm run build && mkdir -p backend/static && cp -r dist/* backend/static/ && pip install -r backend/requirements.txt"
  },
  "start": "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
}
