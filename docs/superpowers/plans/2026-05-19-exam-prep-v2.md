# 备考小站 V2 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 V1 纯前端应用升级为 FastAPI + SQLite 后端，重做移动端全屏卡片流适配

**Architecture:** React SPA 通过 REST API 与 FastAPI 后端通信，SQLite 存储数据，FastAPI 托管前端静态文件实现单服务部署

**Tech Stack:** Python FastAPI + SQLite + React 19 + Vite 6

---

## 文件结构

```
lianjie/
├── backend/                       # 新建
│   ├── main.py                    # FastAPI 入口 + CORS + 静态文件
│   ├── database.py                # SQLite 连接 + 建表
│   ├── requirements.txt           # fastapi, uvicorn, httpx
│   └── routers/
│       ├── __init__.py
│       ├── todos.py
│       ├── notes.py
│       └── weather.py
├── src/                           # 修改现有
│   ├── api/
│   │   └── client.js              # 新建 - fetch 封装
│   ├── hooks/
│   │   ├── useTodos.js            # 新建 - 替代 useLocalStorage
│   │   ├── useNotes.js            # 新建 - 替代 useLocalStorage
│   │   └── useWeather.js          # 修改 - 改调后端 API
│   ├── components/
│   │   ├── TodoList/TodoList.jsx  # 修改
│   │   ├── StudyNotes/StudyNotes.jsx  # 修改
│   │   └── WeatherCard/WeatherCard.jsx # 修改
│   ├── App.jsx                    # 修改 - 加移动端适配
│   └── index.css                  # 修改 - 移动端 CSS 变量
├── vite.config.js                 # 修改 - 加 proxy
└── package.json                   # 不变
```

---

### Task 1: 后端脚手架

**Files:**
- Create: `backend/requirements.txt`
- Create: `backend/database.py`
- Create: `backend/main.py`
- Create: `backend/routers/__init__.py`

- [ ] **Step 1: 创建 requirements.txt**

```
fastapi==0.115.0
uvicorn==0.30.0
httpx==0.27.0
```

- [ ] **Step 2: 创建 database.py**

```python
import sqlite3
import os

DB_PATH = os.path.join(os.path.dirname(__file__), "exam_prep.db")

def get_db():
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row
    conn.execute("PRAGMA journal_mode=WAL")
    return conn

def init_db():
    conn = get_db()
    conn.executescript("""
        CREATE TABLE IF NOT EXISTS todos (
            id TEXT PRIMARY KEY,
            text TEXT NOT NULL,
            completed INTEGER DEFAULT 0,
            created_at TEXT DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS notes (
            date TEXT PRIMARY KEY,
            content TEXT NOT NULL,
            updated_at TEXT DEFAULT (datetime('now'))
        );
    """)
    conn.commit()
    conn.close()
```

- [ ] **Step 3: 创建 main.py**

```python
import os
from contextlib import asynccontextmanager
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from fastapi.staticfiles import StaticFiles
from database import init_db
from routers import todos, notes, weather


@asynccontextmanager
async def lifespan(app: FastAPI):
    init_db()
    yield

app = FastAPI(title="备考小站 API", lifespan=lifespan)

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(weather.router, prefix="/api")
app.include_router(todos.router, prefix="/api")
app.include_router(notes.router, prefix="/api")

# 托管前端静态文件（生产模式）
static_dir = os.path.join(os.path.dirname(__file__), "static")
if os.path.exists(static_dir):
    app.mount("/", StaticFiles(directory=static_dir, html=True), name="static")
```

- [ ] **Step 4: 创建 routers/__init__.py**（空文件）

- [ ] **Step 5: 安装依赖并验证启动**

```bash
cd backend && pip install -r requirements.txt && python -c "from main import app; print('OK')"
```

---

### Task 2: 天气 API

**Files:**
- Create: `backend/routers/weather.py`

- [ ] **Step 1: 创建 weather.py**

```python
import httpx
from fastapi import APIRouter

router = APIRouter()

WEATHER_URL = (
    "https://api.open-meteo.com/v1/forecast"
    "?latitude=36.06&longitude=103.79"
    "&current=temperature_2m,relative_humidity_2m,weather_code"
    "&daily=temperature_2m_max,temperature_2m_min,weather_code"
    "&timezone=Asia/Shanghai&forecast_days=1"
)

WEATHER_MAP = {
    0: ("晴", "☀️"), 1: ("晴间多云", "🌤️"), 2: ("多云", "⛅"),
    3: ("阴天", "☁️"), 45: ("雾", "🌫️"), 48: ("雾凇", "❄️"),
    51: ("小毛毛雨", "🌦️"), 53: ("毛毛雨", "🌧️"), 55: ("大毛毛雨", "🌧️"),
    61: ("小雨", "🌦️"), 63: ("中雨", "🌧️"), 65: ("大雨", "🌧️"),
    80: ("阵雨", "🌦️"), 81: ("中阵雨", "🌧️"), 82: ("大阵雨", "🌧️"),
    95: ("雷阵雨", "⛈️"), 96: ("雷阵雨伴冰雹", "⛈️"), 99: ("大雷暴", "⛈️"),
}

@router.get("/weather")
async def get_weather():
    async with httpx.AsyncClient() as client:
        resp = await client.get(WEATHER_URL, timeout=10)
        resp.raise_for_status()
        data = resp.json()
    code = data["current"]["weather_code"]
    desc, icon = WEATHER_MAP.get(code, ("未知", "🌈"))
    return {
        "temp": data["current"]["temperature_2m"],
        "humidity": data["current"]["relative_humidity_2m"],
        "description": desc,
        "icon": icon,
        "high": data["daily"]["temperature_2m_max"][0],
        "low": data["daily"]["temperature_2m_min"][0],
    }
```

- [ ] **Step 2: 验证天气 API**

```bash
cd backend && python -c "
from fastapi.testclient import TestClient
from main import app
client = TestClient(app)
r = client.get('/api/weather')
print(r.status_code, r.json())
"
```

---

### Task 3: 待办事项 API

**Files:**
- Create: `backend/routers/todos.py`

- [ ] **Step 1: 创建 todos.py**

```python
from fastapi import APIRouter, HTTPException
from pydantic import BaseModel
from database import get_db

router = APIRouter()

class TodoCreate(BaseModel):
    text: str

class TodoUpdate(BaseModel):
    completed: bool

@router.get("/todos")
def list_todos():
    db = get_db()
    rows = db.execute("SELECT * FROM todos ORDER BY created_at").fetchall()
    db.close()
    return [dict(r) for r in rows]

@router.post("/todos")
def create_todo(body: TodoCreate):
    import uuid
    db = get_db()
    todo_id = str(uuid.uuid4())
    db.execute("INSERT INTO todos (id, text) VALUES (?, ?)", (todo_id, body.text))
    db.commit()
    row = db.execute("SELECT * FROM todos WHERE id=?", (todo_id,)).fetchone()
    db.close()
    return dict(row)

@router.patch("/todos/{todo_id}")
def update_todo(todo_id: str, body: TodoUpdate):
    db = get_db()
    row = db.execute("SELECT * FROM todos WHERE id=?", (todo_id,)).fetchone()
    if not row:
        db.close()
        raise HTTPException(404, "Todo not found")
    db.execute("UPDATE todos SET completed=? WHERE id=?", (int(body.completed), todo_id))
    db.commit()
    row = db.execute("SELECT * FROM todos WHERE id=?", (todo_id,)).fetchone()
    db.close()
    return dict(row)

@router.delete("/todos/{todo_id}")
def delete_todo(todo_id: str):
    db = get_db()
    row = db.execute("SELECT * FROM todos WHERE id=?", (todo_id,)).fetchone()
    if not row:
        db.close()
        raise HTTPException(404, "Todo not found")
    db.execute("DELETE FROM todos WHERE id=?", (todo_id,))
    db.commit()
    db.close()
    return {"ok": True}
```

- [ ] **Step 2: 验证待办 API**

```bash
cd backend && python -c "
from fastapi.testclient import TestClient
from main import app
c = TestClient(app)
# Create
r = c.post('/api/todos', json={'text': 'test'})
assert r.status_code == 200
tid = r.json()['id']
# List
assert len(c.get('/api/todos').json()) >= 1
# Toggle
r = c.patch(f'/api/todos/{tid}', json={'completed': True})
assert r.json()['completed'] == 1
# Delete
assert c.delete(f'/api/todos/{tid}').json()['ok']
print('All todos tests passed')
"
```

---

### Task 4: 备考记录 API + 倒计时 API

**Files:**
- Create: `backend/routers/notes.py`

- [ ] **Step 1: 创建 notes.py（含笔记 + 倒计时）**

```python
from fastapi import APIRouter, HTTPException
from pydantic import BaseModel
from database import get_db
from datetime import datetime

router = APIRouter()

class NoteSave(BaseModel):
    date: str
    content: str

@router.get("/notes")
def list_notes(date: str = None):
    db = get_db()
    if date:
        row = db.execute("SELECT * FROM notes WHERE date=?", (date,)).fetchone()
        db.close()
        return dict(row) if row else {}
    rows = db.execute("SELECT * FROM notes ORDER BY date DESC").fetchall()
    db.close()
    return [dict(r) for r in rows]

@router.post("/notes")
def save_note(body: NoteSave):
    db = get_db()
    db.execute(
        "INSERT OR REPLACE INTO notes (date, content, updated_at) VALUES (?, ?, ?)",
        (body.date, body.content, datetime.now().isoformat())
    )
    db.commit()
    row = db.execute("SELECT * FROM notes WHERE date=?", (body.date,)).fetchone()
    db.close()
    return dict(row)

@router.delete("/notes/{date}")
def delete_note(date: str):
    db = get_db()
    db.execute("DELETE FROM notes WHERE date=?", (date,))
    db.commit()
    db.close()
    return {"ok": True}

@router.get("/countdown")
def get_countdown():
    target = datetime(2026, 12, 19)
    now = datetime.now()
    diff = target - now
    if diff.total_seconds() <= 0:
        return {"days": 0, "passed": True}
    return {
        "days": diff.days,
        "hours": diff.seconds // 3600,
        "minutes": (diff.seconds % 3600) // 60,
        "seconds": diff.seconds % 60,
        "passed": False,
        "target": "2026-12-19",
    }
```

- [ ] **Step 2: 注册路由到 main.py**

```python
# 在 main.py 中已有的 import 行确保包含:
from routers import todos, notes, weather
# 在 include_router 部分确保已有:
app.include_router(notes.router, prefix="/api")
```

- [ ] **Step 3: 验证笔记和倒计时 API**

```bash
cd backend && python -c "
from fastapi.testclient import TestClient
from main import app
c = TestClient(app)
# Notes
r = c.post('/api/notes', json={'date': '2026-05-19', 'content': 'test'})
assert r.status_code == 200
r = c.get('/api/notes?date=2026-05-19')
assert r.json()['content'] == 'test'
# Countdown
r = c.get('/api/countdown')
assert r.json()['days'] > 0
print('All notes/countdown tests passed')
"
```

---

### Task 5: Vite 代理配置 + 前端 API 层

**Files:**
- Modify: `vite.config.js`
- Create: `src/api/client.js`
- Create: `src/hooks/useTodos.js`
- Create: `src/hooks/useNotes.js`
- Modify: `src/hooks/useWeather.js`

- [ ] **Step 1: 修改 vite.config.js 加代理**

```javascript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': 'http://localhost:8000',
    },
  },
});
```

- [ ] **Step 2: 创建 src/api/client.js**

```javascript
const BASE = '/api';

async function request(method, path, body) {
  const opts = {
    method,
    headers: { 'Content-Type': 'application/json' },
  };
  if (body) opts.body = JSON.stringify(body);
  const res = await fetch(`${BASE}${path}`, opts);
  if (!res.ok) throw new Error(`API error: ${res.status}`);
  return res.json();
}

export const api = {
  get: (path) => request('GET', path),
  post: (path, body) => request('POST', path, body),
  patch: (path, body) => request('PATCH', path, body),
  delete: (path) => request('DELETE', path),
};
```

- [ ] **Step 3: 创建 src/hooks/useTodos.js**

```javascript
import { useState, useEffect, useCallback } from 'react';
import { api } from '../api/client';

export default function useTodos() {
  const [todos, setTodos] = useState([]);
  const [loading, setLoading] = useState(true);

  const fetchTodos = useCallback(async () => {
    try { setTodos(await api.get('/todos')); } catch (e) { console.warn(e); }
    finally { setLoading(false); }
  }, []);

  useEffect(() => { fetchTodos(); }, [fetchTodos]);

  const addTodo = async (text) => {
    const todo = await api.post('/todos', { text });
    setTodos(prev => [...prev, todo]);
  };

  const toggleTodo = async (id) => {
    const todo = todos.find(t => t.id === id);
    const updated = await api.patch(`/todos/${id}`, { completed: !todo.completed });
    setTodos(prev => prev.map(t => t.id === id ? updated : t));
  };

  const deleteTodo = async (id) => {
    await api.delete(`/todos/${id}`);
    setTodos(prev => prev.filter(t => t.id !== id));
  };

  return { todos, loading, addTodo, toggleTodo, deleteTodo };
}
```

- [ ] **Step 4: 创建 src/hooks/useNotes.js**

```javascript
import { useState, useEffect, useCallback } from 'react';
import { api } from '../api/client';

export default function useNotes() {
  const [notes, setNotes] = useState([]);
  const [loading, setLoading] = useState(true);

  const fetchNotes = useCallback(async () => {
    try { setNotes(await api.get('/notes')); } catch (e) { console.warn(e); }
    finally { setLoading(false); }
  }, []);

  useEffect(() => { fetchNotes(); }, [fetchNotes]);

  const saveNote = async (date, content) => {
    const note = await api.post('/notes', { date, content });
    setNotes(prev => {
      const idx = prev.findIndex(n => n.date === date);
      if (idx >= 0) return prev.map(n => n.date === date ? note : n);
      return [...prev, note];
    });
  };

  const getNoteByDate = (date) => notes.find(n => n.date === date);

  return { notes, loading, saveNote, getNoteByDate };
}
```

- [ ] **Step 5: 修改 src/hooks/useWeather.js**

```javascript
import { useState, useEffect } from 'react';
import { api } from '../api/client';

export default function useWeather() {
  const [state, setState] = useState({ loading: true, error: null, data: null });

  async function fetchWeather() {
    setState({ loading: true, error: null, data: null });
    try {
      const data = await api.get('/weather');
      setState({ loading: false, error: null, data });
    } catch (err) {
      setState({ loading: false, error: err.message, data: null });
    }
  }

  useEffect(() => { fetchWeather(); }, []);

  return { ...state, refresh: fetchWeather };
}
```

---

### Task 6: 改造前端组件

**Files:**
- Modify: `src/components/TodoList/TodoList.jsx`
- Modify: `src/components/StudyNotes/StudyNotes.jsx`
- Modify: `src/components/WeatherCard/WeatherCard.jsx`

- [ ] **Step 1: 改造 TodoList.jsx** — 用 `useTodos` 替代 `useLocalStorage`

```javascript
import { useState } from 'react';
import useTodos from '../../hooks/useTodos';
import styles from './TodoList.module.css';

export default function TodoList() {
  const { todos, loading, addTodo, toggleTodo, deleteTodo } = useTodos();
  const [text, setText] = useState('');

  function handleAdd() {
    const trimmed = text.trim();
    if (!trimmed) return;
    addTodo(trimmed);
    setText('');
  }

  function handleKeyDown(e) {
    if (e.key === 'Enter') handleAdd();
  }

  const activeTodos = todos.filter(t => !t.completed);
  const completedTodos = todos.filter(t => t.completed);

  return (
    <div className="card">
      <h3 className={styles.heading}>📝 待办事项</h3>

      <div className={styles.inputRow}>
        <input
          className={styles.input}
          type="text"
          value={text}
          onChange={e => setText(e.target.value)}
          onKeyDown={handleKeyDown}
          placeholder="🐣 添加备考任务..."
        />
        <button className={styles.addBtn} onClick={handleAdd}>+</button>
      </div>

      {loading && <p className={styles.empty}>加载中...</p>}
      {!loading && todos.length === 0 && (
        <p className={styles.empty}>还没有待办事项 ✨ 加一个吧~</p>
      )}

      <ul className={styles.list}>
        {[...activeTodos, ...completedTodos].map(todo => (
          <li key={todo.id} className={`${styles.item} ${todo.completed ? styles.completed : ''}`}>
            <button
              className={`${styles.checkbox} ${todo.completed ? styles.checked : ''}`}
              onClick={() => toggleTodo(todo.id)}
            >
              {todo.completed ? '❤️' : '🤍'}
            </button>
            <span className={styles.todoText}>{todo.text}</span>
            <button className={styles.deleteBtn} onClick={() => deleteTodo(todo.id)}>✕</button>
          </li>
        ))}
      </ul>
    </div>
  );
}
```

- [ ] **Step 2: 改造 StudyNotes.jsx** — 用 `useNotes` 替代 `useLocalStorage`

```javascript
import { useState } from 'react';
import useNotes from '../../hooks/useNotes';
import { todayStr } from '../../utils/dateUtils';
import styles from './StudyNotes.module.css';

export default function StudyNotes() {
  const { saveNote, getNoteByDate } = useNotes();
  const [selectedDate, setSelectedDate] = useState(todayStr());
  const [content, setContent] = useState(() => {
    const entry = getNoteByDate(todayStr());
    return entry ? entry.content : '';
  });

  function switchDate(dateStr) {
    setSelectedDate(dateStr);
    const entry = getNoteByDate(dateStr);
    setContent(entry ? entry.content : '');
  }

  function handleSave() {
    saveNote(selectedDate, content.trim());
  }

  const hasContent = content.trim().length > 0;
  const isToday = selectedDate === todayStr();

  return (
    <div className="card">
      <h3 className={styles.heading}>📖 每日备考记录</h3>

      <div className={styles.dateRow}>
        <input
          type="date"
          className={styles.dateInput}
          value={selectedDate}
          onChange={e => switchDate(e.target.value)}
          max={todayStr()}
        />
        {!isToday && (
          <span className={styles.dateHint}>查看历史记录</span>
        )}
      </div>

      <textarea
        className={styles.textarea}
        value={content}
        onChange={e => setContent(e.target.value)}
        placeholder={isToday ? "今天学了什么？记录一下吧 🪷" : "该日期暂无记录"}
        rows={5}
      />

      {isToday && (
        <button className={styles.saveBtn} onClick={handleSave}>
          {hasContent ? '💾 保存记录' : '📝 开始记录'}
        </button>
      )}

      {!isToday && !hasContent && (
        <p className={styles.empty}>这一天还没有记录哦 🪷</p>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 改造 WeatherCard.jsx** — 无需改动 UI 层，hook 已改。确认导入路径仍为 `../../hooks/useWeather`

---

### Task 7: 移动端全屏卡片流适配

**Files:**
- Modify: `src/index.css`
- Modify: `src/App.module.css`
- Modify: `src/components/TodoList/TodoList.module.css`
- Modify: `src/components/StudyNotes/StudyNotes.module.css`
- Modify: `src/components/WeatherCard/WeatherCard.module.css`
- Modify: `src/components/Countdown/Countdown.module.css`
- Modify: `src/components/Header/Header.module.css`

- [ ] **Step 1: 修改 index.css** — 加移动端基础样式

在 `index.css` 末尾追加：

```css
@media (max-width: 640px) {
  body {
    font-size: 16px;
  }

  .card {
    padding: 20px 16px;
    border-radius: 16px;
    margin: 0 0 16px;
  }
}
```

- [ ] **Step 2: 修改 App.module.css** — 移动端全屏卡片流

```css
.app {
  max-width: 960px;
  margin: 0 auto;
  padding: 0 16px 30px;
}

.grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
  margin-top: 20px;
}

.footer {
  text-align: center;
  padding: 24px 0 10px;
  color: var(--color-text-secondary);
  font-size: 0.9rem;
}

@media (max-width: 640px) {
  .app {
    padding: 0 12px 20px;
  }

  .grid {
    display: flex;
    flex-direction: column;
    gap: 16px;
    margin-top: 16px;
  }

  .footer {
    font-size: 0.85rem;
    padding: 20px 0 8px;
  }
}
```

- [ ] **Step 3: 修改 Header.module.css** — 移动端适配

```css
.header {
  text-align: center;
  padding: 30px 0 10px;
}

.title {
  font-size: 2rem;
  color: var(--color-pink-dark);
  font-weight: 700;
  letter-spacing: 2px;
}

.date {
  margin-top: 6px;
  font-size: 0.95rem;
  color: var(--color-text-secondary);
}

@media (max-width: 640px) {
  .header {
    padding: 20px 0 8px;
  }

  .title {
    font-size: 1.4rem;
    letter-spacing: 1px;
  }

  .date {
    font-size: 0.9rem;
  }
}
```

- [ ] **Step 4: 修改 Countdown.module.css** — 移动端适配

在文件末尾追加：

```css
@media (max-width: 640px) {
  .number {
    font-size: 1.8rem;
  }

  .block {
    min-width: 48px;
  }

  .sep {
    font-size: 1.3rem;
  }
}
```

- [ ] **Step 5: 修改 TodoList.module.css** — 移动端触摸友好

在文件末尾追加：

```css
@media (max-width: 640px) {
  .input {
    padding: 10px 16px;
    font-size: 16px;
  }

  .addBtn {
    width: 44px;
    height: 44px;
    font-size: 1.4rem;
  }

  .item {
    padding: 12px 4px;
  }

  .checkbox {
    font-size: 1.3rem;
    min-width: 44px;
    min-height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .deleteBtn {
    font-size: 1rem;
    min-width: 44px;
    min-height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .todoText {
    font-size: 16px;
  }

  .list {
    max-height: none;
  }
}
```

- [ ] **Step 6: 修改 WeatherCard.module.css** — 移动端适配

在文件末尾追加：

```css
@media (max-width: 640px) {
  .icon {
    font-size: 2rem;
  }

  .temp {
    font-size: 1.8rem;
  }

  .details {
    gap: 12px;
    font-size: 0.9rem;
  }
}
```

- [ ] **Step 7: 修改 StudyNotes.module.css** — 移动端触摸友好

在文件末尾追加：

```css
@media (max-width: 640px) {
  .textarea {
    font-size: 16px;
    padding: 14px;
    min-height: 120px;
  }

  .dateInput {
    font-size: 16px;
    padding: 8px 14px;
    min-height: 44px;
  }

  .saveBtn {
    padding: 12px 0;
    font-size: 1rem;
    min-height: 48px;
  }
}
```

---

### Task 8: 端到端验证

- [ ] **Step 1: 安装 Python 依赖**

```bash
cd backend && pip install -r requirements.txt
```

- [ ] **Step 2: 启动后端**

```bash
cd backend && python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

- [ ] **Step 3: 启动前端（新终端）**

```bash
npm run dev
```

- [ ] **Step 4: 访问 http://localhost:5173 验证**
  - 4 张卡片正常渲染
  - 待办事项可增删改
  - 备考记录可保存和查看
  - 天气数据正常显示
  - 倒计时正常走动
  - 移动端视图 (Chrome F12 手机模拟) 卡片全屏宽度
  - 无控制台错误

- [ ] **Step 5: 验证 API 直连**

```bash
# 先 Vite build 生成静态文件
npm run build
# 复制到 backend/static
cp -r dist/* backend/static/
# 直接访问后端确认静态文件托管
curl http://localhost:8000/
```

---

### Task 9: 部署配置

**Files:**
- Create: `backend/Procfile`（Railway）
- Create: `render.yaml`（Render）

- [ ] **Step 1: 创建 Railway Procfile**

```
web: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
```

- [ ] **Step 2: 创建 Render render.yaml**

```yaml
services:
  - type: web
    name: exam-prep-station
    env: python
    buildCommand: cd backend && pip install -r requirements.txt && cd .. && npm install && npm run build && cp -r dist backend/static
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
```
